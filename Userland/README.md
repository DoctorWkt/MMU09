# MMU09 Userland Code

This will eventually contain the userland code for the MMU09 platform.
That is, the system programs and libraries, e.g. `cp`, `mv`, `fprintf()` etc.

I will attempt to use the 2.11BSD userland code as it's a 16-bit platform.
