// This comes from Minix. I've brought it in so that I don't have to
// manually remove all the prototypes.
#ifndef _ANSI_H
#define _ANSI_H

#define _PROTOTYPE(function, params)    function params
#define _ARGS(params)                   params

#endif
